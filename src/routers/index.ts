import orderRouter from './order.router';
import productRouter from './product.router';
import userRouter from './user.router';

const routers = { productRouter, userRouter, orderRouter };

export default routers;
